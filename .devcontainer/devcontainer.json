{
    "name": "Conda + Nextflow + Java",
    "build": {
        "dockerfile": "Dockerfile",
        "context": "."
    },
    "remoteUser": "root",
    "postCreateCommand": "conda init bash",
    "customizations": {
        "vscode": {
            "extensions": []
        }
    }
}
