{
    "name": "Conda + Nextflow",
    "build": {
        "dockerfile": "Dockerfile",
        "context": "."
    },
    "remoteUser": "root",
    "postCreateCommand": "conda init bash",
    "customizations": {
        "vscode": {
            "extensions": []
        }
    }
}

